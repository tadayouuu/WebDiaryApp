@page
@model LockoutModel
@{
    ViewData["Title"] = "アカウントがロックされとるで";
}

<div class="identity-card text-center">
    <h2 class="fw-bold mb-3">@ViewData["Title"]</h2>
    <p class="text-muted mb-4">
        何回もログインに失敗したけぇ、<br />
        アカウントが一時的にロックされとるみたいじゃ。<br />
        しばらく時間をおいてから、もう一度試してくれんさい。
    </p>

    <div class="mt-4">
        <a asp-page="./Login" class="btn btn-dark rounded-pill px-4 py-2">
            ログインページへ戻る
        </a>
    </div>
</div>
